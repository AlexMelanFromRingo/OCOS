return {
  id          = "ocos.desktop",
  name        = "Desktop",
  version     = "0.1.0",
  description = "OCOS desktop shell — wallpaper, status bar, dock.",
  authors     = { "OCOS contributors" },
  license     = "MIT",
  prefix      = "/",
  files       = {
    -- File checksums are filled in by tools/pack.py before signing.
    -- The desktop ships from the OS image so its files aren't tracked here.
  },
  entry       = "Main.lua",
  -- The desktop is the host shell, not a user-launchable app — keep it
  -- out of the launcher menu so clicking "Desktop" doesn't try to spawn
  -- a second instance inside the running session.
  hidden      = true,
  caps_required = { "ui:compositor" },
}
