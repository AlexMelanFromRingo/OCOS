return {
  id          = "sessiond",
  description = "Interactive session manager (TTY login + shell)",
  exec        = "/sys/svc/sessiond.lua",
  after       = { "logd" },
  caps        = { "*" },
  restart     = "always",
  autostart   = true,
}
