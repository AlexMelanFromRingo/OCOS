return {
  id          = "ocos.robot",
  name        = "OCOS robot pack",
  version     = "0.1.1",
  description = "Autonomous robot tasks: farm, mine, sort, tree, tunnel, stair, fill, quarry, build.",
  authors     = { "OCOS contributors" },
  license     = "MIT",
  prefix      = "/",
  include     = { "bin/**" },
  -- pack.py rewrites this with sha256 + size for every shipped file.
  files       = {},
  -- The pack itself isn't a runnable app, just a bundle of CLI tools.
  entry       = "",
  caps_required = {
    "syscall:exec",
    "syscall:write:/*",
    "component:robot",
    "component:inventory_controller",
  },
}
